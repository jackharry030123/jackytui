name: RDP + Call Operator Fix + VRDP

on:
  workflow_dispatch:
    inputs:
      tailscale_key:
        description: 'Tailscale auth key (reusable). Leave blank to use local .env or env var on runner.'
        required: false
        default: ''

jobs:
  setup-rdp-and-vm:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      # ==========================================
      # PHASE 1: PREP & RDP
      # ==========================================
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          netsh advfirewall firewall add rule name="RDP-Tailscale-Host" dir=in action=allow protocol=TCP localport=3389
          netsh advfirewall firewall add rule name="RDP-Tailscale-VM" dir=in action=allow protocol=TCP localport=33389
          Restart-Service -Name TermService -Force

      - name: Create RDP User
        run: |
          $password = 'Zain.rty123'
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          if (Get-LocalUser -Name "Administrator" -ErrorAction SilentlyContinue) {
              Set-LocalUser -Name "Administrator" -Password $securePass
          } else {
              New-LocalUser -Name "Administrator" -Password $securePass -AccountNeverExpires
          }
          Add-LocalGroupMember -Group "Administrators" -Member "Administrator" -ErrorAction SilentlyContinue
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "Administrator" -ErrorAction SilentlyContinue
          echo "RDP_CREDS=$password" >> $env:GITHUB_ENV

      - name: Keep Session Active
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fResetBroken" -Value 0
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "IdleTimeout" -Value 0
          powercfg.exe /change monitor-timeout-ac 0
          powercfg.exe /change standby-timeout-ac 0

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i `"$installerPath`" /quiet /norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        env:
          TS_AUTH_KEY: ${{ github.event.inputs.tailscale_key }}  # from workflow input
        run: |
          $tsPath = "$env:ProgramFiles\Tailscale\tailscale.exe"

          # fallback: allow TS_AUTH_KEY passed via env on runner or local .env file
          if (-not $env:TS_AUTH_KEY) {
              $envFile = "$env:GITHUB_WORKSPACE\.env"
              if (Test-Path $envFile) {
                  Write-Host "Reading .env fallback for TS_AUTH_KEY"
                  $lines = Get-Content $envFile
                  foreach ($l in $lines) {
                      if ($l -match '^\s*TS_AUTH_KEY\s*=\s*(.+)\s*$') {
                          $env:TS_AUTH_KEY = $matches[1].Trim()
                          break
                      }
                  }
              }
          }

          if (-not $env:TS_AUTH_KEY) {
              Write-Error "No Tailscale auth key provided. Supply as workflow input or set TS_AUTH_KEY in runner environment or .env file (not committed)."
              exit 1
          }

          $authKey = $env:TS_AUTH_KEY
          # Mask show
          Write-Host "Using tailscale key with prefix:" + $authKey.Substring(0,5) + "..."
          & $tsPath up --authkey=$authKey --hostname="gh-runner-$env:GITHUB_RUN_ID" --unattended

          # Wait for IP assignment
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 20) {
              $tsIP = & $tsPath ip -4
              if ($tsIP) { break }
              Write-Host "Waiting for Tailscale IP... ($retries/20)"
              Start-Sleep -Seconds 3
              $retries++
          }

          if (-not $tsIP) {
              Write-Error "Tailscale IP not assigned. If this fails, generate a NEW Reusable key."
              exit 1
          }
          
          Write-Host "Tailscale Connected! IP: $tsIP"
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      # ==========================================
      # PHASE 2: INSTALLATION (VirtualBox & Extension Pack)
      # ==========================================
      - name: Set VirtualBox Global Config Path
        run: |
          New-Item -Path "C:\VirtualBoxConfig" -ItemType Directory -Force
          [Environment]::SetEnvironmentVariable("VBOX_USER_HOME", "C:\VirtualBoxConfig", "Machine")

      - name: Download VirtualBox Installer & Extension Pack
        run: |
          $dest = "$env:USERPROFILE\Downloads"
          if (-not (Test-Path $dest)) { New-Item -ItemType Directory -Path $dest | Out-Null }

          Write-Host "Downloading VirtualBox Installer..."
          Invoke-WebRequest `
            -Uri "https://download.virtualbox.org/virtualbox/7.2.4/VirtualBox-7.2.4-170995-Win.exe" `
            -OutFile "$dest\VirtualBox-Installer.exe" `
            -UseBasicParsing

          Write-Host "Downloading Extension Pack with exact filename..."
          curl -L -o "$dest\Oracle_VM_VirtualBox_Extension_Pack-7.2.4.vbox-extpack" "https://pub-4e3db5ccea8a4f30ae7d4ee15e71f566.r2.dev/Oracle_VM_VirtualBox_Extension_Pack-7.2.4.vbox-extpack"

      - name: Install VirtualBox & Extension Pack
        run: |
          $dest = "$env:USERPROFILE\Downloads"
          $vbInstaller = "$dest\VirtualBox-Installer.exe"
          $extPack = "$dest\Oracle_VM_VirtualBox_Extension_Pack-7.2.4.vbox-extpack"

          Write-Host "Installing VirtualBox..."
          Start-Process -FilePath $vbInstaller -ArgumentList "--silent --ignore-reboot --msiparams ALLUSERS=2" -Wait

          $env:Path += ";C:\Program Files\Oracle\VirtualBox"
          $VBoxManage = "C:\Program Files\Oracle\VirtualBox\VBoxManage.exe"

          if (Test-Path $VBoxManage) {
              Write-Host "VirtualBox installed. Installing Extension Pack..."
              & $VBoxManage extpack install --replace $extPack --accept-license=56adf0d4f1ba0e3f3372bd3c79d0f8b9
              Write-Host "Installed Extension Packs:"
              & $VBoxManage list extpacks
          } else {
              Write-Error "VirtualBox installation failed."
              exit 1
          }

      # ==========================================
      # PHASE 3: VM DEPLOYMENT
      # ==========================================
      - name: Download OVA
        run: |
          $dest = "$env:USERPROFILE\Downloads"
          Write-Host "Downloading OVA to $dest..."
          Invoke-WebRequest `
            -Uri "https://pub-4e3db5ccea8a4f30ae7d4ee15e71f566.r2.dev/Nadeem.ova" `
            -OutFile "$dest\Nadeem.ova" `
            -UseBasicParsing

      - name: Import and Enable VRDP
        run: |
          $VBoxManage = "C:\Program Files\Oracle\VirtualBox\VBoxManage.exe"
          $dest = "$env:USERPROFILE\Downloads"
          $ovaPath = "$dest\Nadeem.ova"
          $vmName = "NadeemVM"

          Write-Host "Importing OVA..."
          & $VBoxManage import "$ovaPath" --vsys 0 --vmname "$vmName" --eula accept

          Write-Host "Enabling VRDP on Port 33389..."
          & $VBoxManage modifyvm "$vmName" --vrde on
          & $VBoxManage modifyvm "$vmName" --vrdeport 33389
          & $VBoxManage modifyvm "$vmName" --vrdeaddress 0.0.0.0
          & $VBoxManage modifyvm "$vmName" --vrdeauthtype null

          Write-Host "Starting VM..."
          & $VBoxManage startvm "$vmName" --type headless

          Start-Sleep -Seconds 10
          & $VBoxManage showvminfo "$vmName" | Select-String "VRDE"

      - name: Connection Info
        run: |
          Write-Host "`n=============================================="
          Write-Host "       VRDP CONNECTION DETAILS"
          Write-Host "=============================================="
          Write-Host "Target: $($env:TAILSCALE_IP):33389"
          Write-Host "==============================================`n"
          while ($true) {
              Start-Sleep -Seconds 300
          }
